package com.wrb.devica.question.domain.option;

import com.wrb.devica.question.domain.OptionCode;

public enum BuildWait implements OptionCode {

    OFTEN,      // 자주 답답했다
    FINE,       // 참을 만했다 / 신경 안 쓰였다
    UNKNOWN     // 잘 모르겠다
}
