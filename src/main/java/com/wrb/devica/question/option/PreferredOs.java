package com.wrb.devica.question.option;

import com.wrb.devica.question.OptionCode;

public enum PreferredOs implements OptionCode {

    MACOS,      // 맥
    WINDOWS,    // 윈도우
    UNDECIDED   // 아직 안 정했다 — Mac·Windows 권장안을 함께 보여준다
}
