package com.wrb.devica.question.option;

import com.wrb.devica.question.OptionCode;

public enum Overheating implements OptionCode {

    OFTEN,      // 자주 그렇다
    FINE,       // 가끔 / 별로 없다
    UNKNOWN     // 잘 모르겠다
}
