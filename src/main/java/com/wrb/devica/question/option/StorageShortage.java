package com.wrb.devica.question.option;

import com.wrb.devica.question.OptionCode;

public enum StorageShortage implements OptionCode {

    OFTEN,          // 자주 있다
    ONCE_OR_TWICE,  // 한두 번 있다
    NEVER           // 없다
}
