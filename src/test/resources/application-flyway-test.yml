spring:
  datasource:
    url: "jdbc:mysql://localhost:${TEST_DB_PORT:3307}/devica_flyway_test?createDatabaseIfNotExist=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul"
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    clean-disabled: false

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  sql:
    init:
      mode: never
