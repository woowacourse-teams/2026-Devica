spring:
  datasource:
    url: jdbc:mysql://localhost:3307/devica_test?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    # main 의 application.yml 은 테스트에서 아예 로드되지 않으므로 여기 다시 적는다
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: true
