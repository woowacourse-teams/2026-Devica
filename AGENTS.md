# AGENTS.md

이 파일은 모든 요청에 주입된다. 작업 종류와 무관하게 걸리는 규칙만 둔다.

## 프로젝트

Devica — 사용자가 **제품 → 사용 목적 → 권장 사양** 순으로 구매 조건을 확정하고,
그 조건에 맞는 전자제품을 검색·필터·정렬해서 확인하는 서비스.

Spring Boot + Thymeleaf **SSR**이다. 별도 프론트엔드 앱이 없으므로 **화면 작업이 컨트롤러
계층까지 이어진다.** 마크업만 고치고 끝나는 작업은 거의 없다. 화면에 필드 하나를 추가하려면
응답 객체·서비스·컨트롤러가 함께 바뀐다.

## 함께 읽을 문서

`harness/rules/` 문서는 해당 영역을 건드리기 **전에 반드시 연다.**

| 문서 | 언제 여나 |
|---|---|
| [`harness/rules/스타일.md`](harness/rules/스타일.md) | **CSS·마크업을 건드리기 전에** |
| [`harness/rules/빌드.md`](harness/rules/빌드.md) | **의존성·설정 파일을 건드리기 전에** |
| [`harness/workflows/add-page.md`](harness/workflows/add-page.md) | 화면을 추가할 때 |
| [`docs/MVP_유스케이스.md`](docs/MVP_유스케이스.md) | 기능·화면·용어를 다룰 때. 여기 없는 것은 만들지 않는다 |
| [`docs/wireframes/`](docs/wireframes/) | 화면 구성과 필드 배치를 확인할 때. 화면별 png가 있다 |
| [`docs/design/디자인_시스템.md`](docs/design/디자인_시스템.md) | 색·간격·타이포·컴포넌트 값이 필요할 때. frontmatter가 기준이다 |
| [`docs/팀_컨벤션.md`](docs/팀_컨벤션.md) | 커밋·브랜치·PR·이슈 |
| [`docs/개발_환경_가이드.md`](docs/개발_환경_가이드.md) | 실행·테스트 방법과 설정 파일의 근거 |
| [`harness/README.md`](harness/README.md) | 규칙을 추가·수정할 때 |

## 계층 경계

데이터는 **컨트롤러 → Model → 템플릿** 한 방향으로만 흐른다.

- 컨트롤러는 **서비스 호출 한 번, Model 담기, 뷰 이름 반환**으로 끝난다
- 포맷팅·조건 분기·연관 데이터 조합은 **서비스에서 끝낸다**
- Model에는 **화면 전용 record 응답 객체만** 담는다. 엔티티를 넘기지 않는다
- 템플릿은 필드를 **출력만** 한다 (반복과 표시/미표시 분기는 허용)
- 템플릿에서 리포지토리·서비스를 직접 호출하지 않는다

> 엔티티 금지는 취향이 아니다. `src/main/resources/application.yml`이 `open-in-view: false`라
> 렌더링 시점에는 영속성 컨텍스트가 이미 닫혀 있다. 엔티티를 Model에 넘기면 템플릿이 지연 로딩
> 필드에 닿는 순간 예외가 난다. 테스트 설정에도 같은 값이 있다.

### 파일 배치 판단 기준

- 한 도메인의 **컨트롤러·서비스·응답 record는 같은 패키지**에 둔다. 계층별 패키지로 나누지 않는다
- `templates/pages/` — **라우트와 1:1**이고 경로를 그대로 따른다. 라우트가 없으면 pages가 아니다
- `templates/fragments/` — 라우트가 없는 조각
- `static/css/components/` — 디자인 시스템에 **이름이 있는 것만**

디렉터리 구조는 [`harness/workflows/add-page.md`](harness/workflows/add-page.md)에 있다.
없는 디렉터리는 필요해질 때 만든다. 미리 만들지 않는다.

## 금지

- 템플릿에서 값을 **계산·가공**하는 표현식
- 공통 레이아웃 fragment 없이 전체 HTML을 중복 작성
- **요청하지 않은 리팩토링.** 발견하면 고치지 말고 보고한다
- 임의의 라이브러리·CDN 추가 (`build.gradle` 수정 포함)
- `docs/MVP_유스케이스.md`에 없는 기능·화면·필드 추가

## 스펙 태깅 게이트

작업 계획의 각 결정 항목에 태그를 붙인다.

- **[HUMAN]** — 문서·코드·설정에 근거가 있다
- **[AI]** — 근거가 없거나 추측이다

**[AI]가 하나라도 있으면 구현을 시작하지 않고 멈춘다.**

### 멈출 때 내놓을 것

1. 태깅한 항목을 **전부** 목록으로 보여준다. [HUMAN]도 포함한다
2. [AI] 항목마다 **무엇을 모르는지**와 **답해야 할 질문**을 함께 적는다
3. 답을 기다린다. 그동안 [HUMAN] 항목만 먼저 구현하지 않는다

```
- [HUMAN] 라우트 `/` — MVP_유스케이스.md §6 표
- [HUMAN] 표시 필드: 제품 종류 목록 — docs/wireframes/제품_선택.png
- [AI] 준비 중 배지의 노출 조건 — §7-12에 배지 규칙은 있으나 어느 제품이 대상인지 없음
  → 질문: MVP에서 선택 가능한 제품이 무엇인가?
```

[HUMAN]까지 전부 내놓는 이유는 **근거를 잘못 댄 [HUMAN]이 [AI]보다 위험하기** 때문이다.
자신 있게 틀린 채로 통과한다. 목록으로 나와야 사람이 "그 파일에 그런 내용 없다"를 잡을 수 있다.

근거를 댈 때는 **파일 이름을 밝힌다.** "일반적으로 그렇게 한다", "보통 이렇게 씁니다"는
근거가 아니다. 근거로 댄 파일에 그 내용이 실제로 있는지 확인하고 인용한다.

**태깅 대상**

Model 응답 객체 구조 · URL·라우팅 · fragment 파라미터 구조 ·
폼 전송·검증 실패 정책 · 에러·빈 상태 정책

**태깅 대상 아님** (판단해서 진행한다)

변수명·메서드명 · fragment 내부 마크업 분리 · CSS 클래스명·스타일 값

### 아직 결정되지 않은 것

관련 작업은 자동으로 **[AI]** 다. 추측해서 채우지 말고 멈추고 물어본다.

- **REST 적용 범위** — 지금은 컨트롤러가 뷰를 반환한다. API로 열 범위는 정해지지 않았다
- **온보딩 4단계 라우트** — `MVP_유스케이스.md` §6의 경로는 "제안"이라고 명시되어 있다 (§12에도 미확정으로 남아 있다)
- **로딩 최소 표시 시간** — 같은 문서 §8.1의 A안/B안이 미정이다
