# AGENTS.md

이 레포에서 작업하는 코딩 에이전트(Codex, Claude Code 등)를 위한 상시 지침이다.

## 프론트엔드 작업 흐름

`frontend/`를 고칠 때만 해당한다.

1. 구현 초안을 완성한다.
2. `review` Skill을 실행한다. Skill이 규칙별로 리뷰하고, 지적을 반영하고, 린트를 돌린 뒤 `.review/`에 기록을 남긴다.
3. 코드와 리뷰 기록(`.review/*.md`)을 함께 커밋한다.

pre-push 훅과 CI는 리뷰 기록의 diff 해시를 현재 코드와 비교한다. 리뷰를 거치지 않았거나 리뷰 뒤에 코드가 바뀌었으면 push와 PR이 막힌다.

## 검사에 막혔을 때

검사를 통과시키는 것이 아니라 검사가 가리키는 문제를 고치는 것이 목적이다.

- **pre-commit이 실패하면** 남은 에러를 읽고 맥락에 맞게 코드를 고친 뒤 다시 커밋한다. `--unsafe`로 한꺼번에 고치지 않는다. 이 옵션은 동작을 바꿀 수 있다.
- **push가 리뷰 기록 때문에 막히면** `review` Skill을 다시 실행한다. 해시만 새로 계산해 기록을 고치지 않는다.
- **훅을 `--no-verify`로 건너뛰지 않는다.** 건너뛰어도 CI가 같은 검사로 막는다.
